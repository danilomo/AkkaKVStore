---
- hosts: all
  become: yes
  become_user: root
  become_method: sudo
  gather_facts: False
  vars:
    ansible_python_interpreter: /usr/bin/python3
  tasks:
    - name: Ansible remove file glob
      file:
        path: /opt/AkkaKVStore
        state: absent










